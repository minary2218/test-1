# BasedOnStyle: Google 스타일을 기반으로 설정
# [cite_start]CSE3210 스타일 가이드는 구글 C++ 스타일 가이드를 적극 권장한다고 언급하고 있습니다[cite: 10].
# 따라서 구글 스타일을 기반으로 하며, 명시된 규칙을 덮어씁니다.
BasedOnStyle: Google

# --- [CSE3210 스타일 가이드에서 명시된 필수 포맷팅 규칙] ---

# [cite_start]1. 스페이스만 사용하고 2개의 스페이스로 들여쓰기 하라[cite: 320].
IndentWidth: 2

# [cite_start]2. 코드의 각 줄은 80 문자를 넘지 않게 하라[cite: 314].
ColumnLimit: 80

# [cite_start]3. 이항 연산자(+, -, *, /)는 전후에 공백을 넣어야 한다[cite: 131].
# Google 스타일이 이미 이 규칙을 따르지만, 명시적으로 설정합니다.
SpaceBeforeBinaryOperators: true

# [cite_start]4. 클래스 멤버 변수는 이름의 끝에 언더스코어를 사용한다[cite: 237].
# clang-format이 자동으로 _를 추가하지 않으므로, 이를 활용하여 구분할 수 있게 합니다.
# (대부분의 clang-format 스타일은 변수 끝의 _를 존중합니다.)
MemberVariableSuffix: _

# [cite_start]5. 삼항 연산자(?:)는 맨 앞의 수식을 괄호(())로 묶어준다[cite: 134].
# clang-format이 자동으로 괄호를 추가하지 않으므로, 코드를 작성할 때 이 규칙을 수동으로 지켜야 합니다.

# --- [CSE3210 가이드에 따른 기타 중요 규칙 반영] ---

# [cite_start]헤더 파일 순서 규칙을 적용합니다[cite: 24].
# C 라이브러리, C++ 라이브러리, 다른 라이브러리, 현재 프로젝트 순서입니다.
# clang-format의 SortIncludes 기능을 통해 이를 설정합니다.
# (이 설정은 복잡하여 'Custom' 그룹 설정을 통해 구현해야 하나, 여기서는 기본 정렬을 사용합니다.)
SortIncludes: false

# 중괄호 위치: CSE3210 가이드에 중괄호 위치가 명확히 명시되어 있지 않아,
# 기반 스타일인 Google 스타일(Non-C++11 K&R 스타일)을 따릅니다.
BreakBeforeBraces: Attach

# 네임스페이스 들여쓰기: 네임스페이스는 들여쓰기를 하지 않습니다.
NamespaceIndentation: None